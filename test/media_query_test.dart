// import 'package:mockito/mockito.dart';

// class MockContext extends Mock implements BuildContext {
//   @override
//   Size? get size => Size(40, 60);

//   @override
//   Widget get widget => Container();
// }

void main() {
  // var ctx = MockContext();
  // final size = ctx.size;

  // test("test get width", () {
  //   print(size);
  //   when(MediaQuery.of(ctx)).thenReturn(MediaQueryData(size: ctx.size!));
  //   expect(Toolkit.mediaQuery.getWidth(ctx), size!.width);
  //   // expect(Toolkit.mediaQuery.getWidth(ctx, size: 0.2), size.width * 0.2);
  // });

  // test("test get height", () {
  //   expect(Toolkit.mediaQuery.getHeight(ctx), size!.height);
  //   expect(Toolkit.mediaQuery.getHeight(ctx, size: 0.2), size.height * 0.2);
  // });
}
